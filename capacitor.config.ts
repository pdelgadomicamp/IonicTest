import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pablodelgado.ionictest',
  appName: 'ionicTest',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
